# Librespot config (minimal)
[device]
name = "LoJix FM"
backend = "pipe"
bitrate = 320
initial-volume = 100
device-type = "speaker"

[session]
# Optional: username/password if using legacy auth
# username = "your_spotify_username"
# password = "your_spotify_password"
